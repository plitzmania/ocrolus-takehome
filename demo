#!/bin/sh
set -eu

if ! command -v uv >/dev/null 2>&1; then
  echo "This demo requires uv: https://docs.astral.sh/uv/getting-started/installation/" >&2
  exit 1
fi

exec uv run --extra test --no-editable ocrolus-takehome-showcase "$@"
